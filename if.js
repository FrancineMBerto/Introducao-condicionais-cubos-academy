// se & senao;
// é possível porém não é recomendado usar um if sem usar chaves {} se não poderá não funcionar as segundas linhas após um if ou else. 
const idade = 9;

if (idade >= 18) {
   // é executado caso a condição seja verdadeira;
   console.log("É maior de idade")
} else {
   // é executado caso a condição seja falsa;
   console.log("É menor de idade")
}


console.log("Fim Do Código!")