const temIngresso = true;
// ! inverte o valor da variavel se ela é true virará false se colocar (!) na frente ======> !temIngresso dessa forma. "se não tem ingresso"
if (!temIngresso === false) {
   console.log("Pessoa sem ingresso!")
}

console.log(`variável fora do if temIngresso: ${temIngresso}`)